# --- MurasamePet-Inaba Unified Requirements ---

# UI & Frontend (PyQt5)
PyQt5>=5.15.0
PyQt5-sip>=12.11.0

# Screen Capture & Image Processing
mss>=9.0.0
numpy>=1.24.0
opencv-python>=4.8.0
Pillow>=10.0.0

# Network & API
requests>=2.31.0

# Backend API Server (FastAPI)
fastapi>=0.100.0
uvicorn>=0.23.0
pydantic>=2.0.0

# Optional exact prompt accounting from the retained GGUF; keep the UTF-8
# fallback when this platform cannot install the native package.  On Windows,
# the validated CPU wheel is installed from llama-cpp-python's extra index:
# python -m pip install --only-binary=:all: --extra-index-url https://abetlen.github.io/llama-cpp-python/whl/cpu "llama-cpp-python>=0.3.35"

# Phase 2 local RAG indexing (the embedding model itself is supplied locally)
chromadb>=0.5.0
sentence-transformers>=3.0.0

# Audio & TTS (System dependent, may need ffmpeg on host)
# Note: Audio playback in frontend uses QtMultimedia
